{
  "name": "helpdesk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "client",
    "e2e"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"bun run dev:server\" \"bun run dev:client\"",
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev",
    "build": "bun run --cwd server build && bun run --cwd client build",
    "test": "bun run --cwd client test",
    "test:e2e": "bun run --cwd e2e test",
    "test:e2e:ui": "bun run --cwd e2e test:ui",
    "test:e2e:headed": "bun run --cwd e2e test:headed",
    "typecheck": "bun run --cwd server typecheck && bun run --cwd client typecheck && bun run --cwd e2e typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
